import React from 'react'
import styled from 'styled-components'

const PredictMarketPanel = ({ decisions }) => (
  <div>{decision.yesMarketPredictedPrice} Predict Market</div>
)

export default PredictMarketPanel
